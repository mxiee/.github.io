/*
// var p = document.createElement ( 'p' );

// Déclaration d'une variable contenant un tableau
var a = [11, 22, 33, 44, 55, 77, 99];

console.log(a); // Affiche mon tableau qui se nome "a"
console.log(a[0]); // Affiche la 6ème valeur du tableau (le premier index du tableau est 0)
console.log(a.length); // Affiche la longueur du tableau (le nombre d'élément dans le tableau)
console.log(a[a.length - 1]); // Affiche la dernière valeur du tableau


// Affiche chaque élément de mon tableau avec son index, via une boucle for
for ( var index = 0; index < a.length; index++) {
  console.log(index, a[index]);
};

// Ajoute une nouvelle variable dans le tableau, la valeur 100
a.push(100);
console.log(a);

// Déclaration d'un objet
var b = {
  prop1 : 10,
  prop2 : 250,
  prop3 : "test"
};

console.log(b); // Affiche l'objet b

// On peut créer un objet, un tableau, etc à l'intérieur d'un objet
b.test = {
  a : 1,
  b : 2,
  c : 3
};
console.log(b.test.a);
*/

//////////////////////////////

/*
// Déclaration de fonction
function sayHello(){
  console.log("hello")
}

// Autre déclaration de fonction
var sayHello = function(name){
  console.log("hello " + name)
}

sayHello("John");


var b = {
  prop1 : 10,
  prop2 : 150,
  prop3 : "test",
  sayHello : function(name){
    console.log("hello " + name)
  }
};

b.sayHello("john");
*/
